pipeline {
  agent {
    docker {
image 'node:16-alpine' 
args '-v ${env.WORKSPACE}:/workspace -w /workspace'
}
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}
