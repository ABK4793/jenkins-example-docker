pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
        // Add the 'script' block to correctly run scripted pipeline commands
        script {
          docker.image('node:16-alpine').inside('-v ${env.WORKSPACE}:/workspace -w /workspace') {
            sh 'node --version'
          }
        }
      }
    }
  }
}
